import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-Ecommerce-site',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
